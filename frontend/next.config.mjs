/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  typescript: {
    // Allow builds to proceed even if type-checking fails.
    ignoreBuildErrors: true,
  },
};

export default nextConfig;








