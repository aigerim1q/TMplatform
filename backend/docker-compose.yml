version: "3.9"

services:
  db:
    image: postgres:15
    container_name: tmplatform_db
    environment:
      POSTGRES_USER: tmplatform
      POSTGRES_PASSWORD: tmplatform
      POSTGRES_DB: tmplatform
    ports:
      - "5432:5432"
    volumes:
      - tmplatform_pgdata:/var/lib/postgresql/data

volumes:
  tmplatform_pgdata:
